# new-game-radar plan

## 目标
- 提供 new-game-radar 网页端
- 让页面可通过 80 端口访问
- 页面展示一个可验证的实时内容：当前平台播放游戏观众最多的直播间
- 提供该直播间直达链接
- 页面 UI 保持清新、布局合理，并继续增强猫猫元素
- 新增一个站内“视频宣传”播放器区域
- 下载 3 个约 30 秒的猫猫/狗狗视频并接入网站
- 用户可以手动切换播放哪一个视频
- 在不明显影响执行效率的前提下，尽量及时汇报真实进展，但不能因为汇报而中断当前操作
- 完成后把可访问链接发给用户检查
- 完成后提交并推送 GitHub

## 当前状态
- 数据脚本：`scripts/new_game_radar.py`
- 仓库根目录：`/root/.openclaw/workspace`
- 自动播报：已停用高频噪音模式
- 自动驱动方式：计划文件 + 1 分钟恢复
- 网页原型：`site/index.html`、`site/app.js`、`site/styles.css` 已存在并可工作
- 站点数据：`scripts/generate_new_game_radar_site_data.py` 可生成 `site/data/radar.json`
- 服务状态：80 端口已有静态服务在运行，首页与 JSON 可访问
- GitHub 状态：远端 `origin/main` 已同步到最近旧提交 `3feefad`
- 视频素材：已生成 3 个站内可访问 MP4：`site/media/videos/cat-radar-1.mp4`、`site/media/videos/dog-radar-1.mp4`、`site/media/videos/cat-radar-2.mp4`
- 当前任务进行中：80 端口校验已通过；提交时发现仓库 `.gitignore` 全局忽略了 `/site`，需要改用 `git add -f` 精确纳入站点文件后继续 commit/push

## Phases
1. 搜索并下载 3 个可用的猫猫/狗狗短视频素材（约 30 秒）
2. 将视频文件放入站点可访问目录
3. 设计并实现“视频宣传”播放器区域与切换控件
4. 保持页面整体风格协调，必要时顺带继续微调 UI
5. 验证 80 端口页面与视频资源可访问、可切换播放
6. 把网站链接发给用户检查
7. git commit + push

## 当前步骤
- Phase 7：使用 `git add -f` 精确暂存 `/site` 相关文件，重新执行提交与推送

## 已完成
- 确认项目根目录与主脚本位置
- `scripts/new_game_radar.py --json` 已跑通，输出结构可消费
- 已新增 `topLiveRoom` 输出，包含平台、游戏、主播、标题、热度、直达链接
- 已确认 `site/` 页面骨架存在并可工作
- 已验证 80 端口当前可访问站点与 JSON 数据
- 已完成上一轮猫猫主题看板版本并推送到 GitHub：`3feefad`
- 已记录用户对汇报节奏的新要求：任务完成只汇报一次；新任务尽量及时回报进展，但不要因为回报打断执行
- 已下载并转码 3 个约 30 秒的本地 MP4 视频素材到 `site/media/videos/`
- 已在 `site/index.html` / `site/app.js` 接入视频宣传模块骨架与切换逻辑
- 已完成 `site/styles.css` 中的视频宣传区布局与响应式样式
- 已用本地静态服务验证首页 HTML 片段与 MP4 资源可正常返回
- 已通过 80 端口校验首页返回 200，且页面包含 `promoVideo` 视频播放器节点
- 已通过 80 端口校验 3 个本地 MP4 资源返回 200 且 `Content-Type` 为 `video/mp4`
- 已确认首次提交尝试失败原因：仓库 `.gitignore` 中存在 `/site` 忽略规则

## 下一步
- 用 `git add -f` 暂存目标站点文件
- 提交并推送到 GitHub
- 完成后把可访问链接发给用户检查

## 阻塞
- 无（可通过强制暂存继续） 

## 验收标准
- 页面新增独立视频宣传区域
- 至少有 3 个本地可播放的猫猫/狗狗视频
- 视频时长约 30 秒，用户可手动切换播放对象
- 页面整体仍然协调，且不影响 80 端口访问
- 汇报节奏遵循：进展及时，但不打断操作；完成时只汇报一次

## 执行规则
- 用户新增/修改需求时，先读当前计划，再写回计划，再继续执行
- 不要反复询问“能不能继续”，默认继续推进，除非遇到真实阻塞
- 不允许把“旧任务已完成”误写成“当前新任务已完成"
- 任务完成后默认只汇报一次；进行中可在不打断执行的前提下汇报真实增量进展

## Checkpoint
- 最近旧阶段提交：`3feefad` Polish new-game-radar cat-themed dashboard
- 当前阶段状态：进行中（未完成）
